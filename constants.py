# -*- coding: utf-8 -*-
from os.path import join as join_path
import os


env = {
    "development": "dev",
    "staging": "stg",
    "staging-aws": "stg-aws",
    "production": "prd",
    "production-aws": "prd-aws",
}
